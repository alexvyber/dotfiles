session_name: vyber-dev 
windows:
  - window_name: api
    layout: tiled
    shell_command_before:
      - cd ~/personal/vyber/api/ # run as a first command in all panes
    panes:
      # - shell_command: # pane no. 1
      #     - cd /var/log # run multiple commands in this pane
      #     - ls -al | grep \.log
      # - echo second pane # pane no. 2
      # - echo third pane # pane no. 3
      - echo "strapi pane" # pane no. 4
      # - yarn develop # start strapi
      
  - window_name: front
    layout: tiled
    shell_command_before:
      - cd ~/personal/vyber/front/ # run as a first command in all panes
    panes:
      # - shell_command: # pane no. 1
      #     - cd /var/log # run multiple commands in this pane
      #     - ls -al | grep \.log
      # - echo second pane # pane no. 2
      # - echo third pane # pane no. 3
      # - echo forth pane # pane no. 4
      - yarn dev # start strapi
      

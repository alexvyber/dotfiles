session_name: phoenix
windows:
  - layout: tiled
    window_name: model
    shell_command_before:
      - cd ~/personal/vyber/api/ # run as a first command in all panes
    panes:
      - shell_command:
          - sshpass -p monafe ssh -o StrictHostKeyChecking=no alexs@192.168.1.7
          - echo "shit"
          - ./server

  - window_name: curl
    layout: tiled
    # shell_command_before:
      # - cd ~/personal/vyber/api/ # run as a first command in all panes
    panes:
      - shell_command:
          # - sshpass -p monafe ssh -o StrictHostKeyChecking=no alexs@192.168.1.7
          # - sleep 10
          # - curl localhost:3399
  - window_name: htop
    layout: tiled
    # shell_command_before:
      # - cd ~/personal/vyber/api/ # run as a first command in all panes
    panes:
      - shell_command:
          # - sshpass -p monafe ssh -o StrictHostKeyChecking=no alexs@192.168.1.7
          # - htop

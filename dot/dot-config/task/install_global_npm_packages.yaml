version: '3'
tasks:
  install:
    cmds:
      - npm i -g pnpm
      - npm i -g git-open yarn pnpm git-split-diffs vtop tsc  ts-node prettier tsup rustywind nodemon @nestjs/cli fastify nest fastify-cli typescript@latest oclif
